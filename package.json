{
  "dependencies": {
    "fs": "^0.0.1-security",
    "https": "^1.0.0",
    "ssh2-sftp-client": "^10.0.3"
  },
  "scripts": {
    "deploy": "cd backend && npm run build && cd ../frontend && npm run build && cd .. && node deploy.js"
  }
}
