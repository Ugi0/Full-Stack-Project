{
  "name": "Full Stack Project",
  "description": "A backend for the full stack project",
  "main": "index.js",
  "scripts": {
    "build": "tsc.cmd --build",
    "buildrun": "npm run build && npm run run",
    "run": "cd build && node index.js"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "express": "^4.18.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "cors": "^2.8.5"
  }
}
